////////////////////////////////
// URGENT
//////////////////////////////


///////////////////////////////
// USER VERIFICATION
///////////////////////////////
- VERIFY: duplicate plotter on usb connect shows up on new session but could also exists otherwise.
- VERIFY: Cut agent fails start probe (see ref images in /test folder)
- VERIFY: the plotter connection types (usb direct, cut agent, etc) need large specialized ui medallions/badges with icons on the connected plotter cards so it is clear to the user which connection is which quickly.

///////////////////////////////
// ADMIN VERIFICATION
///////////////////////////////
This checklist is built from your todo list, my notes from the migration, and the current state of the repo. In order:

1. Fix the Vercel environment (blocking, needs you)
- Set STRIPE_CONNECTED_ACCOUNT_ID=acct_1Ta4d8FTK26Xr6hG.
- Make sure STRIPE_WEBHOOK_SECRET matches the webhook endpoint we_1TatCRF… on the connected account. As of yesterday, four events there were still undelivered.
- Once it's fixed, resend those failed events from that endpoint's page in the Stripe dashboard, or rely on the backfill sync in step 3.

2. Deploy before Oct 5
- main already has the billing fix (5fdf49a). dev is two commits ahead with the support tickets, live plan limits, free-month coupons and billing tools. Merge dev into main and deploy.
- The deploy also adds the daily support-sweep cron.
- Oct 5 is when the first migrated Lite subscription bills on the connected account. Oct 19 and 22 follow.

3. After the deploy, in Admin → Billing
- Run the backfill sync. It re-pullsn from the connected account, which  catches anything the broken webhoo
- Open Billing health; every check should be green. If a price check says "no cached price ID", run the sync on Admin → Products.
- For a single account, use Resync billing from Stripe in that user's drawer or on their support     ticket.

4. The three affected Lite customers
- The customer who filed the sample ticket: open it in Admin → Support.                              a. Click Link to account; it's a ling existed.
  b. Click Resync and confirm they show as Lite.
  c. Check the duplicate flag on their charges. Their duplicate was already refunded, so there should be nothing to do.
  d. Send "Our bug, fixed + month on us" with the free month ticked.
- The other two: Admin → Users, open their drawer, then Give free month with "Email the customer" on.
- Timing: each free month comes off their first connected-account invoice (Oct 5, 19 or 22). Those subscriptions must have been created as connected-account subscriptions, which the migration did.
- Still to tell them: the old item from your todo list stands. Their next statement will show the connected account's business name, and mf6m… will see a $29 refund. The ticket reply covers the    customer who filed it; the other th email is a natural place to add it.
- Skip anyone marked compedTier: true. Those accounts were given their plan by hand and aren't billed.
                                                                                                   5. Email and support setup
- Make sure hello@omniplot.app actually receives mail. It now gets every staff notification, the overdue digest and server-error alerts.
- The first support-sweep run will send one "overdue" digest listing every old contact-form submission, since none were answered. Close or answer those in Admin → Support.
                                                                                                   6. Firebase console (from your backl- Turn on Firestore TTL policies forLimits.expiresAt. This can't be donefrom code.

One warning: your local .env has live keys pointing at production Firebase. Running the dev server locally and clicking "Give free montustomers and real Stripe. For a dry
run of the new tools, use a Stripe tase project, or try them first onyour own account in production.

//////////////////////////////////////
// BACKLOG
////////////////////////////////
- translate btn on support ticket (from spanish to english)
- support ticket template "our bug, fixed, + month on us" only shows up sometimes, why is that? it doesnt show up for some (especially in spanish) so probably best to persist a generic version of that template since ill be using it alot during dev stages.
- tooltips need responsive aligning.
- save draft btn on each step of the pattern importer
- pattern uplaoder: ui message and/or reposition to missing or incorrect values on the form instead ofg silently failing the submit.
- upload pattern btn dyancmially changes to incorrect values for the pattern library category filter views such as still showing vehicle veribage when they are clciked into a different category
- audit all advertised features, services, pages (marketing & user dashboard) to make sure we support, enforce, and have the right systems in place for the whole process. update any marketing pages with features we've added since updating those last or process changes. find where we have inaccurate or mock data to prevent misadvertising. build a list of whats incomplete and also what is missing entirely or something we should have considering our reexpanded product line. we need proper disclousure and badges next to everything we dont support or is in dev and disable to prevent misuse or abuse.
- BG remove option (on by default) on pattern uploader input images to help clean up the input image before before for a cleaner vectorization.
- Fixing OmniPlot's autonesting so it uses all patterns without stranding pieces outside the cut zone, even partially so all bounds need to be checked.
- Next: enable the Firestore TTL policy on errorLogs.expiresAt in the Firebase console....One follow-up you'll need to do outside code: enable a Firestore TTL policy on rateLimits.expiresAt (Firebase console → Firestore → TTL) so old window docs get purged automatically — TTL policies can't be set from rules or app code./.
- Create a local plotter emulator/simulator to verify the communication between plotter and agent (or other method) is bidirectional and valid throughout each expected communication event with proper processing and response. we need to test without having the quipment in person.
- 'pattern builder' where the uses inputs the exact dimensions and sides/intersection points with drag option on connecting points to manually craft the pattern directly from the studio. this give true sandbox and potentially unlimited types of patterns.
- reports/support systems needs conversational ability for user and admin (user must be logged in to start this kindof support chat) to address "report an issue" reports directly from the ui from both user and admin. like a persistant message conversation formed from that report an issue message that the user started that admin can change the state on as it progresses.
- multiple usdb connections seemd to have popped up. idk of the same or if it just detected multiple but there was a lot of connections. we should find a way to compress the unactive connections box size to clerarly differiate the the active connected vs inactive methods.
- need optional end cut that cut with enough force to cleanly cut both layers of the roll to separate the job from the master roll completely.
https://www.rvinyl.com/collections/chevrolet-precut-window-tint/products/chevrolet-silverado-2007-2013-2-door-avery-dennison-nr-nano-ceramic-ir-precut-window-tint?metaobject_handle=chevrolet-silverado-precut-window-tint
- verify only after disconnecting usb connection method, agent option card/status show on plotter tab in studio as should with the usb connection method card/status as if to choose a new method now. Since the user disconnects one method, they'll likely want to see the others like in whatever state (if any) they are in.
- Patterns: https://www.thetinteffect.com/precut-window-tint-kits/2014-2018-2019-limited-chevrolet-silverado-1500-truck-visor-eye-brow-precut-tint-kit-automotive-window-film/?srsltid=AfmBOooS3aialyaNfEgmkpu9aVOmeudAYHUx4fMVRufxybaruT3WlFOB
- offer cutting services with simple user flow to select from db of patterns or submit own then submit order. we cut at our shop and send to customer in hardened tube. priced material cost +
- offline mode
- vertical preference for pattern placement to reduce width consumption when the longest size of the pattern fits within the width of the roll.
- custom material selection for unique dimensions (i.e user has some crazy jagged material to use idk)
- Community pattern uploaded need to 1:1 match our admin added patterns. community patterns needs to show unverified until approved by admin or community method. request need to be approved/rejected by admin in under a few hours.
- fully custom pattern upload for custom shapes/logos/icons/etc.. with option to submit to community under a more fitting category than vehicles.
- User should be able to upload and use any vehicles pattern they want with the option to submit to community. if they want to submit to the community they must provide any missing information before submission to gurantee it matches our other pattern datap points.
- Verify/refactor User request of a pattern to be sourced by admin or community, not themselves. Maybe other community members will respond. Upvote system seems like a nice touch in case its a bade design.
- Add flagging for review/report for all patterns (platform or community provided) in case of inaccuracies that warn the users when a pattern is flagged for review when selected to add to the canvas.
- The patterns are wrong. None of the windows are accurate. Our system didnt seem to take the curvature of the windows into consideration.
- HEAT SHRINKAGE: add option/setting for user to add internal buffer to each design to intentional give excess inside the cut zone line a 1cm-ish default (can be changed by user) for user's placements and make up from HEAT SHRINKAGE
- User uploaded 2d scanning tool data output as a pattern.
- turn users phone camera into web-based 2d scanning tool.

/////////////////////////////
// LONG TERM VERIFICATION / NEEDS TESTING
//////////////////////////////
- the user "selected" connection does persist as being selected between page navigation.
- usb direct connection doesnt persist between page navigation or show probe prompt
- the roll measurements displayed and used on the grid ui doesn't is confusingly presented. it says for example 40" in multiple places. the roll size once on the grid is ideal and the amount used. Increase the text size of both to be more prominent.
- Disclaim to user the cutter has to be in manual/offline mode in order to receive jobs.
- TEST - Update the "Send a test job...Use the Manual Test panel above to...." step in setup/confirm that we are not in fact using a 500 unit cut as suggsested as that is against our reworked "1-inch by 1-inch box with a circle in the middle" test cut that we just implemented. it might just be text update here n there but lets double check that we only using ournew small test cut pattern and our other agent related docs/guide to make sure we are up-to-date on that last part (and consistent "/agent", "/studio/agent", etc..). Also the guide on the "/agent" page should be multi-row bc its too clustered on all devices.
- TEST - Plotter origin wizard/settings should inherit the user selected roll size from the properties tab so that the roll alignment wizard shows the correct roll size. right now, it doesnt match. Any cross-setting/data like this and others need to be sync properly to prevent conflicts.
- TEST: The update agent prompt logic for update detection & agent running sync is bugged. A user reported that the versions (v1.1.0) they have match exactly the latest release candidate yet ther version mismatch prompt to upgrade still exists. Another user reported that they are using an old version (v1.0.1) and are not receicing a prompt to udpate. The state from previous running agents is not flushed properly. The new state was not persisted (we recently changed how persisted connections after these reports so this part may have been addressed already so you will need to verify will breaking those updates) Also, Verify the we are not making the user download each missing version in order and repeated the download/delete process entirely until reaching the latest version. We need to make sure the user only has to download the latest. Also we need to verify that the user successfully closed the agent and removed their previous local install of the old version to prevent conflicts. We need to make these agent connection state reactive, persistant, & dynamically cleanable when needed.
- TEST: The selected plotter connect method connection doesnt persist when the user navigates to another tab or page. That user selected connection has to persist the entire session and ideally remember for next time and attempt to auto connect first through the last used method if available then remains if anything is even available at all.
- TEST: Send test cut from plotter is way too BIG. The test cut should be very small 1-inch by 1-inch box with a smaller circle inside of the box.
- TEST: the first pattern for any job completes succesfully but the any additional patterns after the first never even start. Instead of moving on the next pattern in the job's pattern order/queue, it jumps to the end where it makes its final end cut off of the film to seperate it from the rest of the roll. The last step is expected and good but not before the rest of the added patterns to job are finished cutting. we need to make sure that each pattern is properly accounted for and not a single one is forgotten about. We need to verify our job handles recoveries correctly and acknowledges when a job is not complete fully. These are the types of logs admin/user could benefit from seeing the admin dashboard.
- TEST: Also there is likely an origin settings issues. perhaps the 53 inch plotter and the 40 inch roll are not aligned in the correct origin and maybe the 13 inch difference is in the wrong origin. We need a smart way to verify all origins match, even if it requires the user to manually adjustment or relay info from the equipment back to the site if needed. We need a efficient, creative, yet working solution to resolve this. What do you have?

/////////////////////////////////////////////
// TEAM/ORG OVERHAUL
////////////////////////////////////////////

Still outstanding from before: none of the billing flows (pause, cancel-now, delete-account, clear-history, admin pause/unpause) have run against real Stripe test-mode webhooks. That test-mode pass is now more important given the API version change touches the same webhook path. Want me to run it?

// ONGOING
Org / Shop / Team Overhaul — Plan

Current state (confirmed): flat two-tier model, no org entity. User 0..1 — 1 Shop, Shop 1 — many ShopMember, roles hardcoded as ShopRole = owner|manager|tech. Everything is Firestore client-SDK + firestore.rules, no server-side membership API. shopId/shopRole scalars on the user doc are read/written at 116 call sites across the app (settings page, join flow, admin panel, billing) — this rules out a rewrite; the migration must be additive.

Target model

- Organization (new, top of hierarchy): the paying entity. id, name, ownerId, plan, seats, stripeCustomerId, ... — effectively what Shop billing fields are today, moved up one level.
- Shop: becomes orgId-scoped, drops billing fields (now just a location/site record).
- Team: org-level group (not per-shop, not a shop subdivision) whose role grant is scoped to one or more shops, or org-wide. This is the plain reading of "an org having multiple shops and multiple groups of teams" and it's the only shape that keeps permission resolution to one document read (see below) — not attempted as "teams nested in shops."
- Membership doc: orgs/{orgId}/members/{uid} — the single source of truth, holding the resolved permission set + which shopIds it applies to. This is the doc Firestore rules and server routes actually read. Firestore's rules-evaluation budget doesn't support walking org→team→role→permission chains per request, so nothing in rules ever traverses that chain — it's precomputed into this one doc whenever a role/team assignment changes.

Why additive, not a rewrite

Keep shopId/shopRole on the user doc as a "default/last-active shop" UI pointer — every existing consumer (shopStore, settings page, handlePortal("shop"), the join page's shopId equality check, admin's removeShop action) keeps compiling. The membership doc becomes authoritative; the scalar becomes a convenience cache updated alongside it.

The privilege-escalation gap that has to close first

firestore.rules:131–136 lets any signed-in user update a shopInvite doc (that's how accept works today) and reads on it are public. With only 3 shop roles the blast radius is small. Once invites can carry cross-shop/team grants, that same permissive rule lets a user rewrite the invite's role field before accepting themselves in. Rules can't express "can't grant a permission you don't hold," "can't self-escalate," "last owner can't be removed," or org-wide seat limits. So: all membership/role/invite/team mutations move to +server.ts routes using the Admin SDK; Firestore rules become a deny-by-default backstop, not the authorization layer. This is implied by the request but not stated, and is the piece most likely to get under-scoped if skipped.

Billing must be explicitly rescoped, not deferred

stripe-ledger.ts keys everything on metadata.type: 'shop' + shopId, and writes subscription status onto shops/{shopId}. If the org becomes the payer: new metadata shape, a new webhook branch, SHOP_PLAN_LIMITS/SHOP_PRICING_PLANS rekeyed to org, seats reinterpreted as an org-wide pool instead of per-shop — and existing live shop subscriptions need a migration or a compat branch reading either metadata shape. This is the item most likely to break silently in production if left implicit.

Phasing (each phase ships independently)

1. Org entity + membership docs, additive. Add Organization, OrgMember types; backfill one org per existing shop (org.ownerId = shop.ownerId); keep shopId/shopRole scalars working unchanged.
2. Server-side membership/invite/role routes + tightened rules. Move mutations off client SDK; rules become read-mostly + deny-by-default writes.
3. Teams: org-level team entity, scoped grants (shop subset or org-wide), permission resolution written into the OrgMember doc on any change.
4. Billing rescope: org-level Stripe customer/subscription, metadata migration for existing shops, seat semantics.
5. UI: org switcher, shop switcher, team management screens; settings page's "team" tab splits into shop and team management.

todo.txt is currently modified on dev — flagging since it's uncommitted local state, not touched by this plan.
