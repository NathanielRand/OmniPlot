<svelte:head>
	<title>About — OmniPlot</title>
	<meta name="description" content="OmniPlot is professional PPF and window tint cutting software built for the modern shop. No install. Any plotter. Any device." />
	<meta property="og:type" content="website" />
	<meta property="og:url" content="https://omniplot.app/about" />
	<meta property="og:title" content="About — OmniPlot" />
	<meta property="og:description" content="OmniPlot is professional PPF and window tint cutting software built for the modern shop. No install. Any plotter. Any device." />
	<meta property="og:image" content="https://omniplot.app/og-image.png" />
	<meta name="twitter:card" content="summary_large_image" />
	<meta name="twitter:image" content="https://omniplot.app/og-image.png" />
</svelte:head>

<div class="about-page">

	<!-- Hero -->
	<section class="about-hero">
		<p class="about-hero__eyebrow">About OmniPlot</p>
		<h1 class="about-hero__title">Built for shops that cut every day.</h1>
		<p class="about-hero__sub">
			Professional PPF and window tint cutting software that runs in your browser —
			no installs, no dongles, no per-machine licenses.
		</p>
	</section>

	<!-- Problem -->
	<section class="about-section">
		<div class="about-section__inner about-section__inner--split">
			<div class="about-split__text">
				<p class="about-section__eyebrow">The problem</p>
				<h2 class="about-section__title">Legacy software wasn't built for your shop.</h2>
				<p class="about-section__body">
					PPF and window tint installers have been stuck with expensive, Windows-only desktop software
					for decades. Separate licenses for PPF and tint. Per-machine activation keys. Installers that
					break on OS updates. Pattern libraries that charge extra for SVG export. Software that doesn't
					work on an iPad at the bay or a MacBook in the office.
				</p>
				<p class="about-section__body">
					The tools hadn't changed. The industry had.
				</p>
			</div>
			<div class="about-split__stat-group">
				<div class="stat-card">
					<span class="stat-card__num">12,400+</span>
					<span class="stat-card__label">Vehicle patterns</span>
				</div>
				<div class="stat-card">
					<span class="stat-card__num">Any</span>
					<span class="stat-card__label">HPGL plotter supported</span>
				</div>
				<div class="stat-card">
					<span class="stat-card__num">0</span>
					<span class="stat-card__label">Downloads required</span>
				</div>
				<div class="stat-card">
					<span class="stat-card__num">2</span>
					<span class="stat-card__label">Workflows — PPF &amp; tint</span>
				</div>
			</div>
		</div>
	</section>

	<!-- Solution -->
	<section class="about-section about-section--alt">
		<div class="about-section__inner">
			<p class="about-section__eyebrow">The solution</p>
			<h2 class="about-section__title">One browser tab. Every cutter. Every vehicle.</h2>
			<p class="about-section__body about-section__body--wide">
				OmniPlot is fully web-based. Open it on your shop desktop, your iPad at the bay,
				or your MacBook at the front desk — the same job, the same patterns, always in sync.
				Export to HPGL for any plotter, connect directly via Web Serial, or run our lightweight
				Cut Agent for automated sending. No install. No drivers. No license keys.
			</p>

			<div class="about-pillars">
				<div class="pillar">
					<div class="pillar__icon" aria-hidden="true">
						<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
					</div>
					<h3 class="pillar__title">Any device</h3>
					<p class="pillar__desc">Desktop, tablet, iPad, phone. The studio is fully responsive and works everywhere modern browsers run.</p>
				</div>
				<div class="pillar">
					<div class="pillar__icon" aria-hidden="true">
						<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M6 6a3 3 0 100-6 3 3 0 000 6zM6 18a3 3 0 100-6 3 3 0 000 6zM20 4L8.12 15.88M14.47 14.48L20 20M8.12 8.12L12 12"/></svg>
					</div>
					<h3 class="pillar__title">Any plotter</h3>
					<p class="pillar__desc">Roland, Graphtec, Mimaki, USCutter, Summa, VEVOR — if it speaks HPGL, OmniPlot drives it.</p>
				</div>
				<div class="pillar">
					<div class="pillar__icon" aria-hidden="true">
						<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
					</div>
					<h3 class="pillar__title">PPF &amp; tint in one</h3>
					<p class="pillar__desc">Switch between PPF and window tint workflows in the same session. No second subscription, no second tool.</p>
				</div>
				<div class="pillar">
					<div class="pillar__icon" aria-hidden="true">
						<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2M9 11a4 4 0 100-8 4 4 0 000 8zM23 21v-2a4 4 0 00-3-3.87M16 3.13a4 4 0 010 7.75"/></svg>
					</div>
					<h3 class="pillar__title">Built for teams</h3>
					<p class="pillar__desc">Shop plans give every tech their own login under one subscription. Invite members, assign roles, manage everything from one place.</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Patterns -->
	<section class="about-section">
		<div class="about-section__inner about-section__inner--split">
			<div class="about-split__text">
				<p class="about-section__eyebrow">The library</p>
				<h2 class="about-section__title">Patterns you can trust on the first cut.</h2>
				<p class="about-section__body">
					Every pattern in the OmniPlot library is digitized from OEM measurements and verified
					by working installers. Each one includes accurate wrap-edge allowances and is versioned
					so you always know exactly what revision you're cutting.
				</p>
				<p class="about-section__body">
					We cover PPF zones (hood, fenders, bumpers, doors, rockers, mirrors, roof, A-pillars,
					headlights) and window tint zones (all glass, including sunroof and quarter windows)
					for every vehicle in the library. New vehicles are added weekly — most request turnarounds
					happen within 72 hours.
				</p>
			</div>
			<div class="about-split__quote">
				<blockquote class="about-quote">
					<p class="about-quote__text">"I ditched YINK the day I tried OmniPlot. The browser-based workflow means my guys can pull it up on any tablet at the bay."</p>
					<footer class="about-quote__attr">
						<span class="about-quote__name">Marcus T.</span>
						<span class="about-quote__shop">Tampa Bay Wraps · Pro plan</span>
					</footer>
				</blockquote>
			</div>
		</div>
	</section>

	<!-- CTA -->
	<section class="about-cta">
		<h2 class="about-cta__title">Ready to cut smarter?</h2>
		<p class="about-cta__sub">Start free — no credit card, no install, no commitment.</p>
		<div class="about-cta__btns">
			<a href="/signup" class="cta-btn cta-btn--primary">Start cutting free</a>
			<a href="/pricing" class="cta-btn cta-btn--secondary">See pricing</a>
		</div>
		<p class="about-cta__contact">
			Questions? <a href="/support" class="about-cta__link">Contact support →</a>
		</p>
	</section>

</div>

<style>
	.about-page {
		padding-bottom: 80px;
	}

	/* ── Hero ─────────────────────────────────── */
	.about-hero {
		max-width: 760px;
		margin: 0 auto;
		padding: 80px 24px 64px;
		text-align: center;
	}
	.about-hero__eyebrow {
		font-family: var(--font-mono);
		font-size: 0.6875rem;
		font-weight: 600;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--color-brand);
		margin-bottom: 16px;
	}
	.about-hero__title {
		font-size: clamp(2rem, 5vw, 3.25rem);
		font-weight: 800;
		letter-spacing: -0.03em;
		line-height: 1.1;
		margin: 0 0 20px;
	}
	.about-hero__sub {
		font-size: 1.125rem;
		color: var(--text-secondary);
		line-height: 1.65;
		max-width: 560px;
		margin: 0 auto;
	}

	/* ── Sections ─────────────────────────────── */
	.about-section {
		padding: 72px 24px;
		border-top: 1px solid var(--border-subtle);
	}
	.about-section--alt {
		background: var(--bg-surface);
	}
	.about-section__inner {
		max-width: 1100px;
		margin: 0 auto;
	}
	.about-section__inner--split {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 64px;
		align-items: center;
	}
	.about-section__eyebrow {
		font-family: var(--font-mono);
		font-size: 0.6875rem;
		font-weight: 600;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--color-brand);
		margin-bottom: 12px;
	}
	.about-section__title {
		font-size: clamp(1.5rem, 3vw, 2.25rem);
		font-weight: 700;
		letter-spacing: -0.025em;
		line-height: 1.2;
		margin: 0 0 20px;
	}
	.about-section__body {
		font-size: 1rem;
		color: var(--text-secondary);
		line-height: 1.7;
		margin: 0 0 16px;
	}
	.about-section__body--wide {
		max-width: 680px;
	}
	.about-section__body:last-child { margin-bottom: 0; }

	/* ── Stats ────────────────────────────────── */
	.about-split__stat-group {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 16px;
	}
	.stat-card {
		background: var(--bg-surface-2);
		border: 1px solid var(--border-default);
		border-radius: var(--radius-xl);
		padding: 24px 20px;
		display: flex;
		flex-direction: column;
		gap: 4px;
	}
	.stat-card__num {
		font-family: var(--font-display);
		font-size: 2rem;
		font-weight: 800;
		letter-spacing: -0.04em;
		color: var(--text-primary);
		line-height: 1;
	}
	.stat-card__label {
		font-size: 0.8125rem;
		color: var(--text-tertiary);
		line-height: 1.3;
	}

	/* ── Pillars ──────────────────────────────── */
	.about-pillars {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 20px;
		margin-top: 48px;
	}
	.pillar {
		display: flex;
		flex-direction: column;
		gap: 10px;
	}
	.pillar__icon {
		width: 40px;
		height: 40px;
		border-radius: var(--radius-md);
		background: var(--bg-surface-2);
		border: 1px solid var(--border-default);
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--color-brand);
		flex-shrink: 0;
	}
	.pillar__title {
		font-size: 0.9375rem;
		font-weight: 600;
		margin: 0;
	}
	.pillar__desc {
		font-size: 0.875rem;
		color: var(--text-secondary);
		line-height: 1.55;
		margin: 0;
	}

	/* ── Quote ────────────────────────────────── */
	.about-quote {
		background: var(--bg-surface-2);
		border: 1px solid var(--border-default);
		border-left: 3px solid var(--color-brand-dim);
		border-radius: var(--radius-xl);
		padding: 28px 24px;
		margin: 0;
	}
	.about-quote__text {
		font-size: 1.0625rem;
		color: var(--text-primary);
		line-height: 1.6;
		font-style: italic;
		margin: 0 0 16px;
	}
	.about-quote__attr {
		display: flex;
		flex-direction: column;
		gap: 2px;
	}
	.about-quote__name {
		font-size: 0.875rem;
		font-weight: 600;
		color: var(--text-primary);
	}
	.about-quote__shop {
		font-size: 0.8125rem;
		color: var(--text-tertiary);
	}

	/* ── CTA ──────────────────────────────────── */
	.about-cta {
		max-width: 560px;
		margin: 0 auto;
		padding: 80px 24px;
		text-align: center;
		border-top: 1px solid var(--border-subtle);
	}
	.about-cta__title {
		font-size: clamp(1.75rem, 4vw, 2.5rem);
		font-weight: 700;
		letter-spacing: -0.025em;
		margin: 0 0 12px;
	}
	.about-cta__sub {
		font-size: 1rem;
		color: var(--text-secondary);
		margin: 0 0 28px;
	}
	.about-cta__btns {
		display: flex;
		gap: 10px;
		justify-content: center;
		flex-wrap: wrap;
		margin-bottom: 20px;
	}
	.cta-btn {
		padding: 11px 24px;
		border-radius: var(--radius-md);
		font-size: 0.9375rem;
		font-weight: 600;
		text-decoration: none;
		transition: opacity 0.12s;
	}
	.cta-btn:hover { opacity: 0.85; }
	.cta-btn--primary {
		background: var(--color-brand-dim);
		color: #fff;
	}
	.cta-btn--secondary {
		background: var(--bg-surface-2);
		border: 1px solid var(--border-default);
		color: var(--text-primary);
	}
	.about-cta__contact {
		font-size: 0.875rem;
		color: var(--text-tertiary);
		margin: 0;
	}
	.about-cta__link {
		color: var(--text-brand);
		text-decoration: none;
	}
	.about-cta__link:hover { text-decoration: underline; }

	/* ── Responsive ───────────────────────────── */
	@media (max-width: 900px) {
		.about-section__inner--split {
			grid-template-columns: 1fr;
			gap: 40px;
		}
		.about-pillars {
			grid-template-columns: repeat(2, 1fr);
		}
	}
	@media (max-width: 480px) {
		.about-pillars {
			grid-template-columns: 1fr;
		}
		.about-split__stat-group {
			grid-template-columns: 1fr 1fr;
		}
	}
</style>
