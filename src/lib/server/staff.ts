// Internal staff inbox — the destination for new-ticket / customer-reply
// notifications, overdue digests and server-error alerts. Never shown to
// customers: every customer conversation happens in the in-app support
// tickets (/support), so there's no public email address to write to.
export const STAFF_EMAIL = 'hello@omniplot.app';
